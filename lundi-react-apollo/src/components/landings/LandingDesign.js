import React from "react";

const LandingDesign = () => (
  <div className="landing-design">
    <img src="lundi-logo.png" alt="landing" className="landing-design__logo" />
    <p className="landing-design__thoughts">
      The logo is a depiction of my current single life made with illustrator
      ellipse tool, brushes, eraser, eraser and eraser. Even though from afar,
      it looks perfectly normal as a circle, at the end of the day, it still
      needs someone to fill the hole.
    </p>
  </div>
);

export default LandingDesign;
